Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To verify that KubeVirt has started, run:

  kubectl -n {{ .Values.kubevirt.namespace }} get kubevirt kubevirt -o=jsonpath="{.status.phase}"

This should show "Deployed" when all components are ready.

To learn more about KubeVirt, visit:
https://kubevirt.io/user-guide/

To verify the operator deployment, run:

  kubectl -n {{ .Values.kubevirt.namespace }} get deployment virt-operator

For more information on using KubeVirt, please refer to the documentation:
https://kubevirt.io/docs/
